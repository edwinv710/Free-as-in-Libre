<%= form_for(@product_display) do |f| %>
  <% if @product_display.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_display.errors.count, "error") %> prohibited this product_display from being saved:</h2>

      <ul>
      <% @product_display.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
